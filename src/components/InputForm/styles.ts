import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  boxInputs: {
    width: '100%',
  },
  labelInputs: {
    fontSize: 16,
    color: "#FFFFFF",
    paddingBottom: 16,
  },
  placeholderInput: {
    flex: 1,
    alignItems: "center",
    height: 42,
    backgroundColor: "#ffff",
    paddingLeft: 10,
    borderRadius: 20,
    fontSize: 16,
  },
});
