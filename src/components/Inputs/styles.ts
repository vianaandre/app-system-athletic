import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  boxInputs: {
    marginBottom: 10,
  },
  labelInputs: {
    fontSize: 16,
    color: "#FFFFFF",
    paddingBottom: 16,
  },
  placeholderInput: {
    width: '100%',
    alignItems: "center",
    height: 42,
    backgroundColor: "#FFFFFF",
    paddingLeft: 10,
    borderRadius: 20,
    fontSize: 16,
  },
});
